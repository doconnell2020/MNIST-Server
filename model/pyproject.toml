[project]
name = "model"
version = "0.1.0"
description = "PyTorch based model for evaluating MNIST-like images."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.8.0",
]
